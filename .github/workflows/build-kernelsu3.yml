name: Build KernelSU for evergo

on: [workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
        with:
          repository: Xiaomi-MT6833/kernel_xiaomi_evergo
          ref: R

      - name: Setup
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq gcc-aarch64-linux-gnu flex bison libncurses5-dev libssl-dev libelf-dev bc cpio kmod

      - name: Config
        run: |
          export ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu-
          make O=out ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- evergo_defconfig

      - name: Build
        run: |
          export ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu-
          make -j$(nproc) O=out ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- 2>&1 | tee build.log

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: evergo-kernel
          path: out/arch/arm64/boot/
